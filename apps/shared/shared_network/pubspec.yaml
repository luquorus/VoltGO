name: shared_network
description: Shared network layer with Dio, interceptors, and error handling
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  dio_web_adapter: ^2.1.1
  flutter_riverpod: ^2.4.9
  shared_auth:
    path: ../shared_auth

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

