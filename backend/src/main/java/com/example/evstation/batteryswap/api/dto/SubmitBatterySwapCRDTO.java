package com.example.evstation.batteryswap.api.dto;

/**
 * DTO for submitting a battery swap change request.
 * The CR ID comes from the path parameter — no body is required.
 */
public class SubmitBatterySwapCRDTO {
    // No fields needed; the CR ID is a path parameter.
}
